# 印西ニュース プロジェクト

千葉県印西市のニュースまとめサイトを収集・生成・公開する統合プロジェクト。
旧「Claude Code（ローカル）＋Claude Desktop Cowork（クラウド）」の分散構成を廃止し、Claude Code単一プロジェクトに統合した（詳細: `印西ニュース_新プロジェクト仕様書.md`）。

## サイト情報
- URL: https://inzai-news.github.io/news/
- GitHubリポジトリ: https://github.com/inzai-news/news （このフォルダがそのままclone元）
- GA4測定ID: G-89CXHHR0XZ

## ファイル構成
- `sources.json` — 収集元の一元定義（HTMLスクレイピング7件＋RSS11件）
- `pipeline.py` — 収集・重複排除・カテゴリ分類・HTML生成・git publishの統合スクリプト
- `news.json` — 統合ニュースデータ（公開対象、gitで管理）。各記事は`pub_str`(元記事の発表日)に加え、`collected_at`(このパイプラインが初めてその記事を収集したJST ISO時刻)を持つ(2026-08-08追加、`collect`の新規記事採用時・`apply-review`のAI判断採用時・`store-add`時にセット。それ以前から存在する記事にはこのフィールドが無い)。
  - **表示順・「新着」ハイライト(3日以内)・「今日」バッジの基準は「発見日」(`discovery_date()`関数、`collected_at`の日付。無ければ`pub_str`にフォールバック)に統一している**(2026-08-08)。当初は「`pub_str`と`collected_at`の乖離が2日以上の記事だけ並び順を`collected_at`に差し替える」閾値方式を試したが、`.news-item.recent`のハイライト(3日以内をハイライトする`data-pub`属性ベースのクライアントJS判定)は`pub_str`基準のまま変更しておらず、並び順だけ`collected_at`基準にしたため「上位に来たのにハイライトされない記事」が発生する不整合が起きた。そのため`render_item()`の`data-pub`属性の元にする日付と、`build_html()`のソートキーの両方を`discovery_date()`で統一し、ハイライト・バッジ・並び順が常に同じ基準になるようにした。ただし記事本文に表示する日付テキスト(`news-date`欄)は引き続き`pub_str`(元記事の実際の発表日)をそのまま表示する(「いつ見つけたか」と「実際いつの記事か」を分離)。Google News等で元記事の発表から数日遅れて配信される記事でも、収集した当日は「新着」として一番上・ハイライト付きで表示され、数日前の記事群に埋もれない。
- `review_queue.json` — AI判断待ちの記事キュー（gitignore対象、判断後は空になり削除される）
- `ai_check_log.json` — 重複判定AIログ（後から閾値の妥当性を検証するため。加えて、除外(exclude/auto_exclude)済みリンクの記憶としても使われ、`collect`実行時に一度除外判定した記事を再度重複判定・AIレビューにかけないようにする）
- `new_badge.json` — 「新着」バッジの状態（gitで管理）。`{"links": {リンク: 追加日時(JST ISO文字列)}, "last_eligible": {"link":.., "ts":..} or null}`の構造。2026-08-01に「リンクの一覧を都度置き換え」形式から「追加日時を記録し、ビルド時に直近`NEW_BADGE_WINDOW_HOURS`(3時間)以内のものだけバッジ表示」形式に変更した(連続して「サイト更新して」が行われた際、直前の更新で付いたばかりのバッジが次の更新で新規記事が出た瞬間に消えてしまう問題があったため)。`collect`(ルールベース採用分)・`apply-review`(AI判断採用分)・`store-add`(手動登録分)はいずれも`mark_new_badge_links()`を呼び、対象リンクに現在時刻を記録して`links`にunionする(3時間を過ぎた既存エントリはこのタイミングで併せて削除)。`build`側は`active_new_badge_links()`で「現在時刻から3時間以内」のものだけを都度計算してバッジ表示に使う。旧形式(フラットな`{link:ts}`辞書、またはリストのみ)が残っていた場合は`load_new_badge_data()`が`links`として引き継ぐ(`last_eligible`は判定材料が無いためNoneから開始)。
  - **`last_eligible`によるフォールバック**: 2026-08-03追加。「新着」バッジの本来の目的は「最後の更新が新着で分かるようにする」ことなので、直近3時間の新着が印西市役所等の新着除外対象(`NEW_ARRIVALS_EXCLUDE_PUBLISHERS`)しか無かった更新回でも、画面上のバッジが0件にならないようにする仕組み。`mark_new_badge_links()`は追加分のうち除外対象でないものがあれば、その最新1件を`last_eligible`として3時間ウィンドウとは無関係に保持し続ける(除外対象でない新しい記事が来るまで上書きされない)。`build_html()`は通常の`new_links`(3時間以内・除外対象外)が空になった場合のみ、`fallback_new_badge_link()`で`last_eligible`を拾って表示する。
- `開店閉店.txt`（Shift-JIS） — 開店閉店情報の調査対象店舗リスト（gitignore対象、ローカルのみ）
- `.gh_token` — GitHub Fine-grained PAT（gitignore対象）
- `index.html` — 生成物（GitHub Pagesで配信）
- `sitemap.xml` / `robots.txt` — 検索エンジン向け生成物。2026-07-31追加、`build`のたびに再生成される(gitで管理)。単一ページサイトのため`sitemap.xml`は`https://inzai-news.github.io/news/`の1件のみで`lastmod`をbuild実行時のJST日付に更新するだけ。`robots.txt`はこの`sitemap.xml`へのリンクを含む固定内容。`index.html`の`<head>`にも同URLへの`<link rel="canonical">`を追加済み
- `weather_icons/` — ウェザーニューズの天気アイコン画像キャッシュ（gitで管理、`build`時に無いものだけ自動ダウンロード）
- `train_timetable_cache.json` — 北総鉄道の電車時刻表・祝日データのキャッシュ（gitignore対象、ローカルのみ）。30日以内なら再取得せずこれを使う
- `参考/` — 旧Cowork/Code両系統の実体一式（gitignore対象、移行が落ち着いたら削除してよい）

## 「ニュース更新して」と言われた場合の手順

1. `python pipeline.py collect` を実行する。ルールベースで判定可能な記事は自動的に `news.json` に反映され、件数サマリ（新規/更新/変化なし/期限切れ/自動除外/要AI判断）が表示される。
2. `review_queue.json` が生成されたら中身を読み、エントリごとに次を判断する:
   - `needs_dedup_review: true` → `similar_to`（既存採用記事のタイトル）と見比べ、同一ニュースの重複記事なら `decision: "exclude"`、別ニュースなら `decision: "keep"` を記入する。判断が難しい場合はリンク先を実際に開いて内容を確認してよい。`reason` に一言理由を書く。
   - `needs_category: true` → 記事のタイトル・publisher・linkから最も適切なカテゴリを判断し `category_decision` に設定する。カテゴリは以下の7つのいずれか:
     `話題・その他` / `イベント・文化` / `市政・行政` / `開発・暮らし` / `開店・閉店` / `鎌ヶ谷・白井` / `イオンモール千葉ニュータウン`
   - 両方trueの場合は両方判断する。判断が付かない記事は `decision` を空のままにしてよい（次回実行まで据え置かれる）。
   - **政治家個人の街頭演説・選挙活動系の記事は`decision: "exclude"`とする**（2026-07-26追加。選挙ドットコム等が配信する特定候補者の街頭演説告知は、市の行政発表やイベントとは異なる政治活動そのものの宣伝のため採用しない）。
   - **publisherが「日本経済新聞」（日経、`nikkei.com`）の記事は有料記事(paywall)であることが多いため、リンク先を実際に開いて確認する**（2026-08-08追加）。有料記事と判明した場合は`decision: "exclude"`、`reason`に「有料記事のため除外」等と記入する。リンク先が確認できない(Google Newsのリダイレクトが解決できない等)場合は、無理に除外判定せずそのまま`keep`してよい。
     - **千葉日報（`chibanippo.co.jp`）は2026-08-10から`collect`実行時点で自動除外（`COLLECT_EXCLUDE_PUBLISHERS`）するようになったため、review_queueにはそもそも回ってこない**。有料記事の割合が高いことに加え、過去記事(2016年頃)の再配信が新着として再ヒットする事例も確認されたため、AI判断の手間をかけず収集自体から除外する方針に変更した(旧: 2026-08-08にリンク先確認方式を導入したが、2026-08-10にユーザーが実際に開いた2件がいずれも有料記事/過去記事の再配信と判明し、確認方式では取りこぼしがあると判断)。除外時は`ai_check_log.json`に`ai_decision: "auto_exclude"`として記録され、`collect`の出力に「自動除外(publisher)」件数として表示される。
   - 判断を書き込んだら `review_queue.json` をEditツールで上書き保存する。
3. `python pipeline.py apply-review` を実行し、判断結果を `news.json` に反映する（`ai_check_log.json` にも記録される）。
4. `python pipeline.py build` で `index.html` を生成する。
5. `python pipeline.py publish` で git add/commit/push する。
6. 最終的な件数サマリを以下のフォーマットでユーザーに報告する。数値は`collect`の出力（取得記事件数/新規の記事件数/タイトル完全一致の重複{exact_dup_count}/更新{updated_count}/変化なし{unchanged_count}/新規{new_count}/自動除外(重複80%以上){auto_excluded_count}/自動除外(publisher){publisher_excluded_count}/判断待ち{skipped_pending}/除外済みスキップ{skipped_excluded}/新規だが掲載期限切れ{expired_new_count}）と`apply-review`の出力（採用{kept}/除外{excluded}）から組み立てる。

```
======================================

取得記事件数    ：{collectの「取得記事件数」}

(内訳)
新規の記事件数  ：{collectの「新規の記事件数」}
既出記事件数    ：{collectの「更新」+「変化なし」の合計}
重複記事件数    ：{collectの「タイトル完全一致の重複」件数}

(新規採用詳細)
新規採用件数    ：{collectの「新規」+ apply-reviewの「採用」の合計}
内AI判定採用件数：{apply-reviewの「採用」件数}

(除去内訳)
80%類似除去件数 ：{collectの「自動除外(重複80%以上)」件数}
publisher除外件数：{collectの「自動除外(publisher)」件数}
AI判定除去件数  ：{apply-reviewの「除外」件数}
期限切れ除去件数：{collectの「新規だが掲載期限切れ」件数}
その他除去件数  ：{collectの「判断待ち(前回から)」+「除外済みスキップ」の合計}

======================================
```

検算: 「取得記事件数」＝「新規の記事件数」＋「既出記事件数」＋「重複記事件数」。
また「新規の記事件数」＝「新規採用件数」＋「80%類似除去件数」＋「publisher除外件数」＋「AI判定除去件数」＋「期限切れ除去件数」＋「その他除去件数」で一致するはず。
「重複記事件数」は、同一記事が複数のGoogle News検索クエリに重複してヒットした分（`collect`実行時に「タイトル完全一致の重複」として画面に表示される）。
「期限切れ除去件数」は、Google News等から拾われた記事が実際には掲載期限(通常記事3か月/開店閉店6か月)を超えた過去記事の再ヒットだったケース。追加と同時に自動除外され、`ai_check_log.json`に記録されるため次回以降は「除外済みスキップ」に回る。

対話実行・CronCreateスケジュール実行のどちらでもこの手順は共通。スケジュール実行時はグレーゾーン判定・カテゴリ分類を自分（Claude）の裁量で判断してよい（ユーザー確認は不要）。

## 「開店閉店.txt更新したので処理して」と言われた場合の手順

1. `python pipeline.py store-pending` で未処理店舗一覧を取得する（6か月経過した処理済み店舗は自動的にtxtから削除される）。
2. 各店舗についてWeb検索で開店/閉店情報を調査する。
3. 情報が見つかったら `python pipeline.py store-add --store "店名" --date "YYYY年M月D日" --type 開店|閉店|リニューアル --link "URL" --publisher "情報源名"` で登録する（`news.json` に `category: 開店・閉店`, `retention_type: store_event`（6か月保存）として追加される）。ユーザーから直接URL・タイトル・開店日を指定された場合はWeb検索を省略してそのままstore-addしてよい。
4. 特定できなかった店舗は `python pipeline.py store-star --store "店名"` で★を付け、以後の調査対象から外す。
5. 処理後、続けて通常の更新手順（1〜6）を流すかユーザーに確認する。
   - **store_eventの保護**: store-addで登録した記事と同じリンクが、後から別ソース（グルメRSS等）で通常収集されても、`collect`時のマージ処理（`cmd_collect`のprev/item統合部分）は`retention_type: store_event`の既存記事を一切上書きせずスキップする（タイトル・カテゴリ・保存期間すべて手動登録の内容を正とする。2026-07-25追加。以前はカテゴリだけでなくタイトル・出典元(source)まで別ソース側の内容で上書きされ、`kaiten_label()`がタイトル先頭の「【日付 開店】」パターンを検出できず「開店日不明」表示になる不具合があった）。
   - **store_eventとの店名部分一致チェック**: 2026-08-01追加。`store-add`は「【日付 種別】店名」という定型タイトルを生成するため、同じ店舗を扱う自然文タイトルの記事（例:「くるまやラーメン印西木下東店」の号外NET記事）が後から別リンクで収集されても、`title_similarity()`(difflib)だけでは表記が違いすぎて類似度が低く出て見逃してしまう問題があった（実例: 類似度0.37で自動除外(0.8)はおろかAI確認(0.7)にも回らなかった）。そのため`cmd_collect`は候補記事ごとに、直近`recent_pool`内の`store_event`エントリの店名（`extract_store_event_name()`で「【日付 種別】」部分を除いて抽出、空白の有無の表記揺れは正規化して比較）が候補タイトルに部分一致するかも別途チェックし、一致すれば類似度に関わらず`needs_dedup_review: true`にして必ず人手/AI確認に回す（強盗事件等、店名を含むだけで無関係な記事の可能性もあるため自動除外はしない）。

## CronCreateスケジュールについて

- VSCode/Claude Code起動中のみ有効。**7日で自動失効する**ため、失効が近い（3日以内）場合はセッション開始時にユーザーへ知らせ、再登録（「スケジュール再開して」等の指示を待つか、明示的な許可を得た上でCronCreateを再実行）を促すこと。
- スケジュールの内容は「ニュース更新して」の手順（上記）と同一。無人実行のため、グレーゾーン判定・カテゴリ分類はClaude自身の判断で進めてよい。
- 外部Anthropic APIキーは使用しない。追加課金は発生しない。

## 既知の制約

- **Google News URL解決は不可能**: `news.google.com/rss/articles/...` は実際のニュースサイトへリダイレクトを解決できない（bot対策）。`/rss/articles/` → `/articles/` への文字列変換のみ行い、リンク先がGoogle Newsのままであることを許容する。
- **paywallチェック機能は持たない**: 旧`articles_cache.json`は前述の制約により実質機能していなかったため廃止した。
- **標準出力バッファリング**: `pipeline.py`は `sys.stdout.reconfigure(line_buffering=True)` を維持している。
- **アトミック書き込み**: JSON保存は一時ファイル＋`os.replace`＋読み直し検証の方式（`save_json_atomic`）を維持している。
- **開店閉店.txtの文字コード**: Shift-JIS。読み書きは必ずこのエンコーディングで行う（`pipeline.py`側で対応済み）。
- **既存データの移行**: 2026-07-16の再構築時、旧`news.json`等の既存データは引き継がず空から再収集を開始した。
- **「新着」カテゴリ**: 2026-07-27追加。全カテゴリ横断で最新`NEW_ARRIVALS_COUNT`(20)件を表示する疑似カテゴリで、`CATEGORY_ORDER`には含まれず一番先頭に固定表示される。`build_html()`内で日付降順ソート済みの`main_arts`の先頭20件をそのまま複製表示するだけで、元のカテゴリから記事を取り除くことはしない(同じ記事が「新着」と本来のカテゴリの両方に表示される)。`render_item()`を共通利用するため、新着バッジ(`new_badge.json`ベース)・「今日」バッジも同一記事なら両方の表示箇所で同じものが付く。
  - **印西市役所の除外**: 2026-08-01追加。印西市公式RSS(`publisher: "印西市"`、表示名は`PUBLISHER_ALIASES`で「印西市役所」)は記事数が非常に多く、「新着」カテゴリ・新着バッジの両方を埋め尽くしてしまうため、`NEW_ARRIVALS_EXCLUDE_PUBLISHERS = {"印西市"}`に該当するpublisherの記事はどちらの対象からも除外する(`市政・行政`カテゴリ自体には引き続き通常通り表示される。除外は表示上の話で、記事自体の収集・保存は変わらない)。分かりやすさのため、`NEW_ARRIVALS_EXCLUDE_CATEGORIES = {"市政・行政"}`に該当するカテゴリの見出しには「(新着対象外)」を付記する(実際の除外はpublisher単位のため、同じカテゴリ内でもPR TIMES発など除外されない記事が混在し得る)。
- **天気予報表示**: `build`実行時にウェザーニューズ(`weathernews.jp/onebox/tenki/chiba/12231/week.html?tab=4`)から印西市の今日・明日・明後日の3日分をスクレイピングし、時刻に応じて2日分(22時未満は今日・明日、22時以降は明日・明後日)をページ上部の電車時刻表ウィジェットの横に表示する(クリックで同サイトの日次ページ`?tab=3`へ遷移)。このページはVue.js製だがサーバー側で実データがHTMLに埋め込まれているため、JS実行なしで取得可能。今日・明日は`#flick_list_today`内の`div.card`から天気文言・最高/最低気温・降水確率を取得し、明後日は`#flick_list_week`内の行から気温・アイコンのみ取得する(天気文言・降水確率%はこのセクションには無い)。アイコンは絵文字ではなくウェザーニューズの実画像(`wxicon/*.png`)を`weather_icons/`にキャッシュして使い回す(同名ファイルがあれば再ダウンロードしない)。ファイル名は`{コード}_{天気名}.png`(天気文言が取れない場合は`{コード}.png`)。取得失敗時は全体を静かにスキップし、ビルド自体は失敗しない。`news.json`には保存されない（build時の都度取得）。旧実装(Open-Meteo API)は精度への不満から2026-07-20に置き換えた。2026-07-20にウェザーニューズ公式のアイコン一覧ページ(`weathernews.jp/ip/help5/tab_icon.html`)を参照し、掲載されていた33種類のコードを`weather_icons/`へ先取りキャッシュ済み(ラベル付き)。
- **電車時刻表表示**: 2026-07-20にトップ記事のヒーロー表示を廃止し、代わりに電車時刻表ウィジェットを表示するようにした(廃止に伴い、それまでヒーロー扱いだった記事は通常通り自分のカテゴリのグリッドに表示されるだけになった。記事が消えるわけではない)。直近3本を表示。番線は表記が不確実(印西牧の原は例外があり駅ごとに1つの番線に確定できない)なため表示していない。実際に「直近の電車」を判定するのは埋め込みデータではなくブラウザ側JS(閲覧者のPC/スマホがページを開いた時点のJST時刻、1秒ごとに再計算して残り時間をカウントダウン表示)で、平日/土曜・休日の判定は曜日に加えて埋め込んだ祝日リストも見る(祝日は土曜・休日ダイヤ扱い)。当駅始発の電車には●マークを付ける。種別欄は「印西牧の原駅に実際に停車するか」で色分けする(停車=通常色、通過=オレンジ)。2026-07-30に「種別名に「特」が含まれるか」という文字列ヒューリスティックから、`INZAI_MAKINOHARA_STOP_TYPES = {"普通", "特急"}`という実データに基づく判定へ変更した(印西牧の原駅自体のd=1/d=2両方向の発車データを実地確認したところ、この駅から発車する種別は「普通」「特急」のみで、「快速」「快特」「通勤特急」「アクセス特急」「エアポート快特」等はいずれもこの駅を通過することが判明したため。旧ロジックだと「アクセス特急」も「特」を含むため誤って停車扱いになっていた)。
  - **循環切り替え(5グループ)**: 2026-07-21に上り/下り2方向の切り替えボタンを追加し、2026-07-30に「品川・新橋」「東銀座・東日本橋」の2グループを追加、2026-07-31に「印旛日本医大(上り・下り)」を末尾に追加して5グループの循環切り替えに拡張した。グループ定義は`TRAIN_GROUPS`(順序付きリスト)で管理し、ボタンを押すたびに次の要素へ進み、末尾の次は先頭に戻る: 「印西牧の原・千葉ニュータウン中央」(上り)→「浅草橋・新鎌ヶ谷」(下り)→「品川・新橋」(下り)→「東銀座・東日本橋」(下り)→「印旛日本医大(上り・下り)」→(先頭に戻る)。ボタン文言は次に切り替わる先を示す(各グループが持つ`own_toggle_label`を、1つ前のグループのtoggleLabelとして使う)。例: 「上り印西牧の原、千葉ニュータウン発切り替え」「下り浅草橋、新鎌ヶ谷発切り替え」「下り品川、新橋発切り替え」「下り東銀座、東日本橋発切り替え」「印旛日本医大発切り替え」。
    - 初期表示の方向はJST時刻依存: 16:00〜23:59は「浅草橋・新鎌ヶ谷」(下り)、それ以外(0:00〜15:59)は「印西牧の原・千葉ニュータウン中央」(上り)がデフォルト(帰宅時間帯に自動で下りが出るように)。「品川・新橋」「東銀座・東日本橋」「印旛日本医大」はデフォルトには使わず、手動で循環させたときのみ表示される。ページ読み込み時に`defaultGroup()`が判定する。
    - 印旛日本医大(上り・下り、末尾グループ): 唯一「1駅の上り・下り両方を1グループ内に左右で並べる」構成(左が上り、右が下り)。他の駅と違い`hokuso.ekitan.com`側は上り(d=1)しか時刻表が存在せず、下り(成田空港方面)を問い合わせると「指定曜日に停車する列車はありません」エラーになる(北総鉄道自社線としての運行区間がここまでで、先は成田空港線(成田高速鉄道アクセス)の管轄区間になるためと見られる)。そのため上り・下りとも一般のekitan.com(路線コード`682-4`、成田スカイアクセス線扱い)から取得する。このページはd=1/d=2どちらのURLでも同じ2テーブルが出力され、1つ目(table_index0)が京成高砂方面(上り)、2つ目(table_index1)が成田空港方面(下り)。上りは印旛日本医大始発の電車を幅広く含む(西馬込・羽田空港・京成上野方面など)ため行き先フィルタなし、下りは他の下りグループと同じ`NARITA_DIRECTION_INCLUDE_DESTS`フィルタを適用。スカイライナーは`fetch_train_timetable_ekitan()`内の種別名フィルタで元々除外される(実データ上もこの駅の発着一覧には出現しない)。
    - 上り: 印西牧の原駅・千葉ニュータウン中央駅→京成高砂・日本橋方面。`hokuso.ekitan.com`(北総鉄道公式の時刻表サイト)から取得(d=1)。行き先の略字(例:「羽」→羽田空港)や「当駅始発」(ページ内で下線表記になっている電車)はページ内の凡例・HTML構造から都度読み取って変換するため、ダイヤ改正で内容が変わっても追従できる。
    - 下り(共通): 浅草橋・品川・新橋・東銀座・東日本橋はいずれも北総線に属さない(浅草橋・新橋・東銀座・東日本橋は都営浅草線、品川は京急本線)ため、`hokuso.ekitan.com`の検索対象外。一般の`ekitan.com`(駅探)から`fetch_train_timetable_ekitan()`で取得する。このサイトは方面ごとに時刻表テーブルが2つ並んで出力されるが、印西牧の原方面が何番目のテーブルかは駅によって異なる(浅草橋・新橋・東銀座・東日本橋は2つ目=table_index1、品川のみ1つ目=table_index0。品川は京急本線の南側の駅のため「泉岳寺方面(d1)」ページを使い、そのページの1つ目のテーブルが泉岳寺方面≒北総線直通方面になる点に注意)。行き先・種別・当駅始発フラグはいずれも`data-dest`/`data-tr-type`/`data-start`属性から直接取れる(凡例展開が不要)ため、hokuso側より単純。ただし種別がフル表記(例:「アクセス特急」)のためhokuso側の略称(「ア特」)に`EKITAN_TYPE_ABBREV`で変換して揃えている。曜日区分はhokuso側がdw=0(平日)/dw=1(土曜・休日)の2区分なのに対し、ekitan.com側はdw=0(平日)/dw=1(土曜)/dw=2(休日)の3区分。既存の平日/週末2区分ロジックに合わせるため週末側はdw=2(休日)を採用している(土曜と休日で若干本数が異なるが実用上の近似として許容)。
    - 下り(行き先フィルタ): 新鎌ヶ谷駅は北総線内(京成高砂と印西牧の原の間)にあるため、「新鎌ヶ谷に行くことができる電車」=新鎌ヶ谷以遠(成田スカイアクセス線・北総線経由で印西牧の原・成田空港方面)へ向かう電車、と定義する。`NARITA_DIRECTION_INCLUDE_DESTS = {新鎌ヶ谷, 印西牧の原, 印旛日本医大, 成田湯川, 空港第2ビル, 成田空港}`をallowlistとして`fetch_train_timetable_ekitan()`に渡し、これに含まれない行き先(泉岳寺止まり・押上止まり・京成高砂止まりや、京成本線経由で新鎌ヶ谷を通らない京成成田/京成佐倉/宗吾参道/芝山千代田行きなど)は表示しない。2026-07-30に、それまでの除外リスト方式(`ASAKUSABASHI_EXCLUDE_DESTS`、浅草橋専用)から、新駅追加を機に許可リスト方式へ変更した(「新鎌ヶ谷に行ける電車のみ」という要件をより正確かつ駅横断で表現できるため)。
    - スカイライナーは、そもそも都営浅草線・京急本線を経由しない(京成の自社線のみを走る)ため`ekitan.com`側のデータには元々出現しないが、念のため`fetch_train_timetable_ekitan()`内で種別名に「スカイライナー」を含む電車を無条件で除外している。新鎌ヶ谷発のスカイライナー(hokuso側、種別略字「ス」)は`TRAIN_GROUPS`内`shinkamagaya`エントリの`exclude_types`で従来通り除外。
    - サーバー側は4グループ全ての駅カラムを常にHTMLに埋め込み(`train-columns-{グループkey}`、keyは`up`/`down_asakusabashi`/`down_shinagawa`/`down_ginza`)、非表示側は`train-columns-hidden`クラスで隠すのみ。トグルボタンのJSが`TRAIN_GROUP_KEYS`(循環順のkey配列)を見てクラス切り替え・タイトル文言(方面ラベル)・カウントダウン再描画をまとめて行う。
    - ヘッダーのボタン位置はロゴ寄りではなく「最終更新」に近い右寄り(`header{gap:20px}`+`.logo{margin-right:auto}`)。ただしモバイル(`max-width:480px`)では、駅名が増えてボタン文言が長くなった(最長で「上り印西牧の原、千葉ニュータウン発切り替え」)ことで1行に収まらずロゴ「印西ニュース」が折り返ってしまう問題が2026-07-30に発覚したため、モバイルのみ`header{flex-wrap:wrap}`+ロゴ/更新日時を1行目(`order:1`/`order:2`)・ボタンを2行目全幅(`order:3;flex-basis:100%`)の2段レイアウトに変更して解消した。
  - **キャッシュ**: ダイヤ改正は年に数回程度しかないため、`build`のたびに問い合わせるのは過剰と判断し、`load_or_fetch_train_data()`が`train_timetable_cache.json`(gitignore対象、ローカルのみ)に取得結果を保存する。キャッシュが`TRAIN_CACHE_MAX_AGE_DAYS`(30日)以内かつ全駅分(2026-07-31時点で10駅、印旛日本医大は上り・下りで別駅扱い)のデータを含む場合はネットワークアクセスなしでそれを使い、古い/存在しない/駅構成が古い(旧バージョンのキャッシュ等、駅を追加/変更した直後を含む)場合のみ再取得する。再取得が全滅した場合(サイト障害等)は多少古くてもキャッシュがあればそれを使い続ける(空表示になるより古い時刻表の方がまし)。祝日データも同じキャッシュに同梱される。取得失敗時は駅ごとに空リストのまま(その駅だけ表示されない)。
  - **ダイヤ改正検知**: キャッシュが古くなって再取得した際、内容が前回キャッシュと異なっていたら`TRAIN_TIMETABLE_CHANGED`を立て、`build`実行時に「北総鉄道の電車時刻表が前回取得時から変更されています(ダイヤ改正の可能性)」と標準出力に表示する。「ニュース更新して」の手順内でこの出力を見かけたら、通常の件数サマリとは別にユーザーへ一言報告すること。駅構成を変更した直後(旧キャッシュに新駅が含まれない)はこの差分比較をスキップし誤検知を防いでいる。
- **event_end_date（イベント開催終了日）**: `event_end_date`（ISO形式、複数日開催なら最終日）を持つ記事はカテゴリを問わず、通常の掲載期限（3か月/6か月）によらず開催終了日+3日（`EVENT_END_GRACE_DAYS`）を過ぎたら`collect`実行時に削除対象となる（`is_expired()`が優先判定）。新規記事として拾った時点で既に開催終了日+3日を過ぎている場合も同様に除外される。現状この値を設定しているのは「ジョイフル本田千葉ニュータウン店」カテゴリのスクレイパーのみ。日付が取得できなかった記事は通常の掲載期限ロジックにフォールバックする。
